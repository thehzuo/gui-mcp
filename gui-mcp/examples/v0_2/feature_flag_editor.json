{
  "v": "0.2",
  "artifact": "feature_flag_editor",
  "title": "Production Flags",
  "sections": [
    {
      "kind": "dependency_toggle_list",
      "title": "Flags",
      "toggles": [
        { "id": "queue", "label": "Queue delivery", "description": "Routes delivery through workers.", "enabled": true },
        { "id": "retry", "label": "Retry policy", "description": "Retries transient provider failures.", "enabled": true, "depends_on": ["queue"], "warning": "Retry requires queue delivery." },
        { "id": "batch", "label": "Batch sends", "description": "Groups low priority notifications.", "depends_on": ["queue"], "warning": "Batch sends require queue delivery." }
      ]
    }
  ],
  "interactions": [{ "kind": "toggle_with_dependencies" }]
}
