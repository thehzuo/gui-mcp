{
  "v": "0.1",
  "artifact": "code_review_explainer",
  "title": "Patch Review: Artifact Renderer",
  "subtitle": "Security and regression-focused review summary",
  "audience": "reviewer",
  "density": "normal",
  "theme": "neutral",
  "sections": [
    {
      "kind": "summary",
      "items": [
        { "label": "Findings", "value": "2 actionable", "tone": "warning" },
        { "label": "Highest severity", "value": "High", "tone": "danger" },
        { "label": "Confidence", "value": "Medium", "tone": "info" }
      ]
    },
    {
      "kind": "diff_review",
      "title": "Changed Files",
      "files": [
        {
          "path": "render/artifact.py",
          "hunks": [
            {
              "header": "@@ render_artifact_to_html @@",
              "lines": [
                { "type": "context", "old_line": 40, "new_line": 40, "text": "state = spec.model_dump_json()" },
                { "type": "remove", "old_line": 41, "text": "html = f\"<script>{state}</script>\"" },
                { "type": "add", "new_line": 41, "text": "html = f\"<script type='application/json'>{safe_json_script(state)}</script>\"" }
              ]
            }
          ],
          "annotations": [
            { "line": 41, "severity": "danger", "comment": "Raw JSON inside script tags can break out if it contains a closing script sequence." }
          ]
        }
      ]
    },
    {
      "kind": "code",
      "title": "Focused Snippet",
      "language": "python",
      "code": "def safe_json_script(raw: str) -> str:\n    return raw.replace('<', '\\\\u003c')",
      "annotations": [
        { "line": 2, "severity": "info", "comment": "Escaping '<' is the critical script-breakout guard." }
      ]
    },
    {
      "kind": "flow",
      "title": "Review Path",
      "nodes": [
        { "id": "input", "label": "Untrusted spec" },
        { "id": "schema", "label": "Pydantic validation" },
        { "id": "escape", "label": "Escaped rendering" }
      ],
      "edges": [
        { "from": "input", "to": "schema" },
        { "from": "schema", "to": "escape" }
      ]
    },
    {
      "kind": "matrix",
      "title": "Severity x Confidence",
      "x_axis": "Severity",
      "y_axis": "Confidence",
      "cells": [
        { "x": "High", "y": "High", "label": "Script breakout in JSON state", "tone": "danger" },
        { "x": "Medium", "y": "Medium", "label": "Large HTML responses can bloat context", "tone": "warning" }
      ]
    },
    {
      "kind": "checklist",
      "title": "Required Fixes",
      "items": [
        { "text": "Escape JSON state for script embedding", "priority": "high" },
        { "text": "Add regression test for closing script sequence", "priority": "high" }
      ]
    }
  ],
  "actions": [
    {
      "kind": "copy_as_prompt",
      "label": "Copy review prompt",
      "payload": "Address the two renderer security review findings."
    }
  ]
}
